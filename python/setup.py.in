from setuptools import setup

setup(
    name='${PYMPSYM}',
    version='${CMAKE_PROJECT_VERSION}',
    description='${CMAKE_PROJECT_DESCRIPTION}',
    url='${CMAKE_PROJECT_HOMEPAGE_URL}',
    author='${CMAKE_PROJECT_AUTHOR}',
    packages=[''],
    package_dir={'': '.'},
    package_data={'': ['${PYTHON_MODULE_PREFIX}${PYMPSYM}${PYTHON_MODULE_EXTENSION}']}
)
